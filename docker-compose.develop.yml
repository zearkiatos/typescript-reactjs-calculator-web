version: "3.9"

services:
  web:
    build:
      context: .
      dockerfile: develop.Dockerfile
    container_name: typescript-reactjs-calculator-web
    volumes:
      - ".:/app"
    env_file:
      - .env.dev
    ports:
      - "3001:3000"
    tty: true